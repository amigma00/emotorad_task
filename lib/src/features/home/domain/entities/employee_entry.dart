class EmployeeEntry {
  String employeeName;
  DateTime checkIn;
  DateTime checkOut;
  bool isPresent;

  EmployeeEntry({
    required this.employeeName,
    required this.checkIn,
    required this.checkOut,
    required this.isPresent,
  });
}
